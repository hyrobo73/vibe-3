import { PlaceholderPanel } from "../../shared/components/PlaceholderPanel";

export function ExcelAutomationPage() {
  return (
    <PlaceholderPanel
      title="엑셀 업무 자동화"
      items={["xlsx 업로드", "기준 컬럼 선택", "분리/병합 작업 생성", "결과 다운로드"]}
    />
  );
}
